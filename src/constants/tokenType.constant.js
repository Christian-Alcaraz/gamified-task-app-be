const TOKEN_TYPE = {
  ACCESS: 'access',
  CHANGE_PASSWORD: 'change-password',
  REFRESH: 'refresh',
};

const TOKEN = {
  AUTH: 'AUTH_TOKEN',
  GUARD: 'GUARD_TOKEN',
  REFRESH_COOKIE: 'X-Refresh-Token',
};

module.exports = {
  TOKEN_TYPE,
  TOKEN,
};
